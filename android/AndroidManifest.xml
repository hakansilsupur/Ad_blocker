<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.hakansilsupur.adblock"
    android:versionCode="1"
    android:versionName="0.1.0">

    <!--
      minSdk 24 covers Android 7 and later, which is also the floor for
      installing on current Android releases. targetSdk stays at 33 so the
      service does not need Android 14's foreground-service type declarations,
      which have no category that fits a DNS filter.
    -->
    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="33" />

    <!-- Relaying allowed lookups to the upstream resolver, and updating lists. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          BIND_VPN_SERVICE is enforced by the system: only Android itself may
          bind this service, and only after the user confirms the VPN prompt.
        -->
        <service
            android:name=".DnsVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>
    </application>
</manifest>
